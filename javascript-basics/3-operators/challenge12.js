/* Challenge 12: Strict Equality Operator
Task: Using the same variables a and b from the previous challenge, use the strict equality operator to check if they are strictly equal. Store the result in isStrictEqual and print it. */

// SOLUTION

let a = 10;
let b = '10';

let isEqual = a === b;
console.log(isEqual);

// OUTPUT: false