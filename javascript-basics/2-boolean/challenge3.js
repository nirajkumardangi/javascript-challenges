/* Challenge 3: Logical AND Operator
Task: Declare two boolean variables isSunny and isWeekend. Use the logical AND operator to create a new variable goForWalk that is true only if both isSunny and isWeekend are true. Print goForWalk to the console. */

// SOLUTION

let isSunny = true;
let isWeekend = true;
let goForWalk = isSunny && isWeekend;
console.log(goForWalk);

// OUTPUT: true;